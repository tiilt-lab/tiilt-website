{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "sassy",
            "type": "shell",
            "command": "python -m http.server 8000 && sass --watch ./scss:./css --style compressed",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}